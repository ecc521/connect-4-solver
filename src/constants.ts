/**
 * Evaluation coordinate space for the solver engines.
 * These correspond directly to the C++ native constants in `native/Constants.hpp`.
 */

export const SCORE_FORCED_WIN_BASE = 31000;
export const SCORE_NNUE_MAX = 30000;
