import subprocess
import random
import os

valid_positions = []
match_count = 0
generated = set()

while len(valid_positions) < 250:
    # Use our C++ generator to quickly generate a random position!
    # Wait, I don't have a compiled generator script. I can just write a quick C++ generator.
    pass
